# 2026-09-18

## ffmpeg 接入: 定位策略 + 双引擎（承接「ffmpeg 安装好了」）

### 暴露的问题
用户已用 winget 装好 ffmpeg 9.0.1-full_build
(`%LOCALAPPDATA%\Microsoft\WinGet\Links\ffmpeg.exe`)，但
`shutil.which("ffmpeg")` **仍返回 None** —— PATH 是进程启动时的快照，
已运行的后端进程读不到新写入的注册表 PATH。

这同时意味着 V14 那批「有 ffmpeg 就优先用它」的分支**从未真正执行过**。

### 改动
1. **新增 `backend/core/ffmpeg.py`**：`find_ffmpeg()` 按序探测
   显式配置(`ffmpeg_path` / `UWC_FFMPEG`) → PATH → 已知安装位置
   (winget Links / scoop shims / choco bin / `C:\ffmpeg\bin` / 商店)
   → `imageio_ffmpeg`。成功永久缓存；**失败只缓存 30s**，
   故装好无需重启后端即可被发现。配置了但无效的路径不报错，继续往下探测。
2. **`config.py`**：新增 `ffmpeg_path`（相对路径按项目根解析）+ `video_engine`，
   支持 `UWC_FFMPEG` / `UWC_VIDEO_ENGINE` 环境变量。
3. **`video.py`**：全面改用 `find_ffmpeg()`，subprocess 走绝对路径；
   新增三态引擎 `auto` / `ffmpeg` / `builtin`；`-nostats` 抑制进度输出；
   `_ffmpeg_merge` 更名 `_ffmpeg_pull`（原名"合并"有误导性，它做的是拉流）。
4. **`scripts/verify_hls.py` 重写**：旧脚本用假分片（`bytes([i+1]) * n`），
   装上 ffmpeg 后 `auto` 会先走 ffmpeg 拉流，而假分片不是合法 TS
   → **脚本直接失效**。改为用 ffmpeg 现场生成真实 HLS
   (testsrc + sine, libx264 + aac, 关键帧对齐分片边界)，
   并加解码级校验（`-f null -` 完整解一遍）与字节级校验
   （按 m3u8 顺序拼接的 sha256，能抓错序/丢块）。
5. **`scripts/start.py`**：新增 `[5/5] 视频引擎 (ffmpeg)` 自检。
   必须用 `find_ffmpeg()` 而非 `which()`，否则会**误报"未安装"**。

### 验证
- `scripts/verify_hls.py`：**18 项断言全通过**（7 个场景）
  - ffmpeg 引擎 → `.mp4`，完整解码通过，时长 6.037s
  - builtin → 内置器分片 + remux → `.mp4`，完整解码通过
  - builtin 无 ffmpeg → `.ts`，**sha256 与原始分片拼接完全一致**
  - auto 无 ffmpeg → 正确降级；ffmpeg 引擎无二进制 → 正确报错、未静默降级
  - 并发 4 = 3.19s vs 并发 1 = 4.26s；缺片恢复后**只请求 `/seg5.ts`**
- 测试：**78 个用例**通过（59 → 78，新增 `tests/test_ffmpeg.py` 19 个）

### 设计取舍
- `builtin` 只约束**下载**方式，不阻止后续 remux —— 用户装 ffmpeg 是为了拿
  通用容器，没必要因强制内置下载而退回 `.ts`
- ffmpeg 拉流时请求由 ffmpeg 自己发出：`DomainLimiter` 不参与、mirrors 不轮换、
  分片进度只在整文件完成时上报一次。这是 `builtin` 存在的另一个理由
  （限速/镜像敏感站点）

### 遗留
- 未在真实视频站点验证 ffmpeg 拉流（本地 HLS 已覆盖协议与请求头逻辑，
  但真实站点的 AES-128 加密流仍未实测）
- 此前列的 P1/P2 项仍在：探测并发、`.part` 临时文件、magic bytes 校验、
  带宽限速、pHash 近似去重、视频未走 `download_with_mirrors`
- git 结构问题（仓库根是整个 `C:\Users\admin`）仍未处理


---

## V16 产出可交付化（下午）

### 新增模块
- `core/naming.py`：命名模板占位符 `{site}{host}{album}{seq}{seq4}{ext}{type}{id}`，
  支持 `/` 分层。**模板含 `..` 或绝对路径整体拒绝**，不静默改写
  （第一版把 `..` 擦成 `._`，被测试当场抓住）
- `core/manifest.py`：任务结束（含取消/部分失败）都写 `manifest.json`，
  记录 rel path / sha256 / size / resolved_url / content_type / note
- `core/cancel.py`：`TaskCancelled` 单独成模块打破循环依赖
  （downloaders 要识别它，但 task_manager 依赖 downloaders）
- `core/sessions.py` + `api/sessions.py`：headful 登录 + **周期快照** storage_state
  （用户关浏览器后 context 就没了，只在收尾读会拿不到）

### 数据库
- `resources` 增 `filename` / `resolved_url` / `content_type`
- 新表 `watches`：url/collector/interval/next_run_time/enabled/hits/last_task_id
- `TaskManager.is_active(task_id)`：判断 worker 是否仍在收尾

### ⚠️ 关键坑：`except Exception` 吞掉取消信号
- 取消被当普通失败 → `_stream_one` 退避重睡 2s/4s 再重试注定被放弃的请求，
  期间资源卡 `downloading`、半成品留盘
- 修法：所有 `except Exception` 前加 `except TaskCancelled: raise`
  （`downloaders/base.py` 两处、`video.py` 三处）
- 回归用例断言的是**耗时 < 1s**，不是"能抛出"——只断言类型这 bug 照样过
- ⚠️ `cancel()` 会**立刻**把 DB 状态写 cancelled（界面秒响应），worker 还要
  收拾现场；断言"收拾干净"要等 `is_active()` 落下去，不能看 status

### 其他
- ZIP 导出第一版写成"攒进 BytesIO 再吐"（等于整包压内存），已重写为生成器逐条写
- 前端暗色主题变量只有 `--panel-2/--border/--accent/--err/--muted`；
  写新样式前先 grep style.css，别用 `--surface` 这类不存在的
- 冒烟后端要 `--app-dir backend`（main.py 里 `from api.x import y` 需要 backend 在 path）
- 本机有代理时 `curl 127.0.0.1` 会走代理返回 502 → 必须加 `--noproxy '*'`

### Git（问题已解决）
- 已在项目目录 `git init`（分支 main）+ 初始提交 `6d5c63c`，81 个文件跟踪，
  产物（downloads/data/node_modules/dist/__pycache__）全部被 .gitignore 挡住
- 父目录 `C:/Users/admin` 那个仓库**仍不要碰**（不要在那里 add/commit）

### 验证
- pytest **127 用例**（新增 test_naming / test_manifest / test_incremental /
  test_export / test_cancel）
- `scripts/verify_output.py` **33 项断言**：命名落子目录、manifest 溯源、
  ZIP 内容一致、同 URL 二轮下载数 0、订阅闭环、**停止 0.01s 生效且无残留**
- 前端 vite build 72 modules；后端真实启动冒烟 200/404 全对

### 遗留
- 真实站点 AES-128 加密 m3u8 仍未实测
- 早期 P1/P2 仍在：探测枚举并发、magic bytes 校验、带宽限速、pHash 近似去重、
  视频未走 `download_with_mirrors`
