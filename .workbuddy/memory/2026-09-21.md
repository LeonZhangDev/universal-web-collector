# 2026-09-21

## V29 功能扩展 + 前端改版（已提交 c300198）

完成并入库（18 files, +2488/-1624）：

### 后端三接口
- `GET /tasks`：搜索/筛选/分页，返回 `{items,total,page,page_size,pages}`（不再是裸数组）。
- `POST /tasks/batch-create`：逐行预检重复/无效，返回 `BatchTaskOut`。
- `GET /env/diagnose`：python/browser/ffmpeg/disk/download_dir 五项 ok/warn/fail。

### 修复的两个真实缺陷（被 tests/test_endpoints_v2.py 捕获）
1. `api/tasks.py` 漏 import `BatchTaskItemOut` → batch-create 生产 500。
2. list 端点把状态组代号 `"active"` 直接传给 DB（`status IN ('active')` 恒 0 条）→ 加 `STATUS_GROUPS` + `_expand_statuses()` 展开。

### 前端改版
- `api.js` 扩展（listTasks/batchCreateTasks/getEnvDiagnose）+ 下载预设 `presets.js`（localStorage）。
- 环境诊断面板 `EnvDiagnose.vue`、创建区重构 `CreatePanel.vue`（单条/批量 + 高级折叠）。
- 任务表格 `TaskTable.vue`（合并列/中文状态/搜索筛选分页）、详情侧边抽屉 `TaskDetail.vue`。
- 样式统一 `style.css` + 大图预览 `Lightbox.vue` + 全局 `toast.js`/`ToastHost.vue`、`status.js`。

### 验证（前序会话已全绿）
pytest 536 passed / verify_output 40 / verify_hls 18 / selfcheck OK / vite build 173 kB。

### 提交注意事项
- 任务 #68–#77 全部 completed。
- 本机 Bash 受限：`cat`/heredoc 不可用，`git -C <abs>` 解析失败；git 命令直接用项目默认 cwd（已是项目根），提交用多 `-m` 而非 heredoc。
- `uwc-verify` venv 已不存在，全量 pytest 重跑需先 `uv`/重建环境。
