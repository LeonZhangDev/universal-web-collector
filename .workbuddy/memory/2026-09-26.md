# 2026-09-26

## MEMORY.md 瘦身（系统要求）
16960 B → **15203 B**，LF 行尾不变，条目一条没删，只压措辞（坑表单元格、版本史、Git 段）。
教训：这个文件已经是第五次逼近上限 → **新坑一律写 `PITFALLS.md`，这里只加一行索引**。

## V45 对标：先回代码核方向，再搜同类产品
用户要求"先确认有几个方向 → 搜同类 → 给建议"。按 `competitor-feature-absorption` 走完。

### 一、方向 = 6 个（不是功能列表，是六条"用户在做的事"）
| 方向 | 证据（回代码核的） | 同类 |
| --- | --- | --- |
| 1 采集下载 | `/tasks/create`·`batch-create`·`preview`·`/collectors/resolve`·`/fs/browse`·`/login/{job_id}`·`domain_min_interval`·AIMD·全局字节桶·`DomainLimiter`·`core/partials.py`·`downloaders/dash.py` | gallery-dl / yt-dlp / JDownloader2 |
| 2 资源库编目检索 | `GET /library`(11 个筛选键)·`/facets`·`/albums`·`/tags`·`/searches`·`rate`·`favorite`·`bulk-delete`·`normalize`·`verify`·`replay` | Eagle / digiKam / PhotoPrism / TagStudio |
| 3 本地素材只读接入 | `api/local.py` **17** 路由（roots/scan/albums/photos/random/favorites/on-this-day/duplicates/stats）·`core/localalbums.py` 只读承诺 | **Immich External Library**（同一契约）/ PhotoPrism originals |
| 4 去重与体检 | `/library/duplicates`·`verify`·`normalize`·`core/phash.py`·`core/filekind.py`·`core/mediacheck.py`·facets 九个特殊键 | Czkawka / dupeGuru / VisiPics |
| 5 自动化集成 | `/watches`+`interval_minutes`+`next_run`+toggle·`/events`(SSE)·`/webhooks`+HMAC+`last_status`·`/notifications`·`/library/url-archive` | Huginn / n8n / **Immich v3 Workflows(预览)** |
| 6 可观测性 | `/system/gates`(六闸)·`/env/diagnose`·`/tasks/storage`·`selfcheck.py`·`drift_check.py`·`probe_site.py`·1400 用例 | Immich「数据完整性检查」；**这是我们的差异化** |

真实路由 **91** 条：`tasks.py` 70 / `local.py` 17 / `sessions.py` 4。

### 二、回代码核"我以为有/没有"的 —— 四个假阳性（核了才没提错建议）
- **`core/imageinfo.py` 名字像 EXIF，实际只解宽高**（过滤广告图）。"EXIF" 二字只出现在注释"EXIF 缩略图"。→ **EXIF 确认没有**。
- `trash` 命中 = `SKIP_DIRS` 里的 `.trash` 目录排除项 → **回收站/软删除确认没有**。
- `proxy` 命中 376 次 = `core/proxy_health.py`（**网络代理**健康），不是视频转码。
- `map`/`geo` 命中 = Python 内置 `map()` 与 JS → **GPS/地图没有**。
- `face`/`semantic` 只出现在 `.md` 文档里（V42 的"推迟"清单）→ 没有实现。

**差点提错的建议**：想提"视频封面帧"，核 `core/thumbs.py` 发现**早就有**（ffmpeg `-frames:v 1`）。

### 三、确认的缺口（词边界级，非子串）
无：幻灯片 · 以图搜图/相似检索 · 语义/向量搜索 · 人脸 · 分享链接 · 配额 · OCR/全文 · 视频转码 · EXIF · GPS · 回收站 · cron 调度 · XMP 写回。
部分：批量改元数据（只有批量**打标签/打星**，无批量改日期/描述/相册）· Webhook（有 `last_status` 回写，**无重试、无投递历史**）。
筛选键 11 个**全是 AND 语义**，无 NOT；无日期区间；排序档位只有 5 档（added/size/rating/name/type，**无日期档**）。

### 四、同类产品本轮新增的事实（2026）
- **Immich v3（2026-07）**：HLS 实时转码 · **Workflows 工作流（预览）** · 移动端 OCR · 「最近添加」页 · **数据完整性检查** · 移动端幻灯片。
- **PhotoPrism（2026-05）**：人脸换 ONNX 管道 · Vulkan 硬件转码(FFmpeg 8) · 信息侧栏直改元数据 · **批量改日期/地点/标签/描述** · **标签布尔 NOT / AND** · **写 XMP sidecar**（元数据存在库外）· Places 3D 地球。
- **Huginn vs n8n**：n8n 有**内置重试 + 错误工作流**、cron/interval/trigger 三类调度、凭据加密；Huginn 停在 2022-08，强项是 WebsiteAgent 抓取 + Liquid 模板 + 事件去重。

### 五、建议优先级（能纯代码落地的先做）
1. **相似检索**——dHash **已经在库里**，只差"给定一张 → 按汉明距离排序"的接口与"找相似"按钮。数据算好了没暴露出来，性价比最高。⚠️ 阈值与"重复 ≤4"**分开**（相似是另一档语义，别复用常量）。
2. 布尔/排除筛选（PhotoPrism 的 NOT）——沿用 `special` 白名单机制，加 `exclude_*`。⚠️ 命中要计数（第 27 条）。
3. 日期区间 + 日期排序档位。⚠️ 我们**没有 EXIF** → 只有落盘时间，**UI 上必须写明口径**，否则是新的静默错。
4. Webhook 重试 + 投递历史表（⚠️ 新表时刻列 `created_at` REAL）。
5. Watch 加 cron（现在只有 `interval_minutes`，一堆 watch 会挤在同一分钟）。
6. 幻灯片（前端为主，纯体验项）。

**不做但写清最小可做版**：语义搜索（需 ML 模型；最小版 = 把标签/路径纳入全文索引，先做纯代码那半）· 人脸（+ 生物识别合规；最小版 = 人物当命名空间标签，人工打）· 分享链接（需多用户；最小版 = 导出带 manifest 的分享包，V44 已具备形态）· 配额清理（与"只标记不删"冲突；最小版 = 只报占用 Top N）· XMP 写回（与只读承诺冲突；最小版 = 导出包里附 XMP）。

### 六、修正的旧结论
- **第 11 条"`/files/raw` 从没实现过"已过期**：现在 `GET /files/raw` 有实现体，且注释里写了"字面量路由必须排在通配 `/files/{task_id}/{path}` 之前"（FastAPI 路由顺序纪律）。坑的**教训**仍成立，但**举例已失效**。
- `on_this_day` 口径 = 文件修改时间而非拍摄时间，`api/local.py` 里已写明（`localalbums.py:988`）—— 不是遗漏，是**已知的、有文档的取舍**。
### 七、V45 落地（承接上面那六条建议）

后端五项 + 前端一项，全部落地，`tests/test_features_v45.py` **30 项全绿**，
六闸全绿（16/3/139/6/144/192），全量 **1430 收集 = 1424 passed + 6 skipped**。

| 项 | 落点 | 一条纪律 |
| --- | --- | --- |
| 相似检索 | `db.similar_to()` + `GET /library/{id}/similar` | 与重复**双阈值**(12 / 4)，退化指纹挡在比对之外 |
| 排除筛选 | `library_filters` 六个 `exclude_*` | `NOT EXISTS` + `NULL` 保护(`IS NULL OR <>`) |
| 日期区间 | `date_from/date_to` + `date` 排序档位 | 中文写“落盘”(无 EXIF)；上界补 `23:59:59`；形态强制 `YYYY-MM-DD` |
| Webhook 重试 | `core/webhooks.py` + `webhook_deliveries` 表 | 4xx **不重试**；每次尝试**各一行**；删 hook 先删历史(SQLite 外键默认关) |
| Watch cron | 新 `core/cron.py`(零依赖) | 日/周取“或”；周日 0↔6 平移；`7` 归一在展开**之后** |
| 幻灯片 | `Lightbox.vue` | 默认不播；禁用态要能看出为什么；`onUnmounted` 清定时器 |

顺带修掉一个真缺陷: `style.css` 灯箱那组选择器前缀写成 `.lightbox` 而组件根类是
`.lightbox-mask` ⇒ **一整组后代选择器全没生效、控制台无报错**(记成第 30 条)。

改过一次形状: `applied` 从“一串裸键名”改成 `[{key,label}]`，中文只在
`LIBRARY_FILTER_LABELS` 一处翻译，并加断言钉住“每个键都有中文名”。

又踩一次 `sqlite3.Row.get()` —— 只在**有真指纹才走到**的路径上发作。
