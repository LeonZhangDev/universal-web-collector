笔记: 补记 CI 结果(三 job 全绿)与两个 skill 的同步

`8a994c2` 推上去之后 backend / frontend / docker 三 job 全绿, 远端 == 本地 HEAD。

本次"本机绿 / CI 红"的两条对策已同步进 skill, 免得下个仓库再从头推导一遍:
- `github-ci-failure-triage`: 取日志时 302 到 blob storage 必须摘掉 Authorization
  (否则 401); 请求要能重试(本机代理偶发 502); 症状表加一行 —— 红的是"体检基线"
  类断言而本机全绿时, 判据若与平台无关, 差异只可能来自**被扫的文件集合**。
- `verification-gate-design`: 新增 8.5「被扫的对象不在名单里(＝没验到)」,
  含"点名未跟踪文件但不算红"与"代号管红不红、文案管哪里红"两条。
